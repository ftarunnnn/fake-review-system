{
  "name": "fake-review-system",
  "version": "2.0.0",
  "description": "Local mock review generator with user-friendly prototype dashboard",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "reset-db": "node -e \"const fs=require('fs'); fs.writeFileSync('database.json', '[]')\""
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "@faker-js/faker": "^8.4.1"
  }
}